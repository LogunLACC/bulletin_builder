# This file can be left empty.
# Its presence tells Python that the 'ui' directory should be treated as a package,
# which allows you to import its modules (like announcements.py, events.py, etc.)
# from other parts of your application (like main.py).

